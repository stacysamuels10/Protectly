---
phase: 14-content-pages-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/compare/page.tsx
autonomous: true
requirements:
  - CONTENT-02

must_haves:
  truths:
    - "A visitor can navigate to /compare and see a feature comparison table"
    - "The comparison page shows a time savings narrative section"
    - "The comparison page has a CTA to sign up at the bottom"
    - "No pricing information appears on the comparison page"
  artifacts:
    - path: "src/app/compare/page.tsx"
      provides: "Comparison landing page"
      contains: "ComparisonPage"
  key_links:
    - from: "src/app/compare/page.tsx"
      to: "/api/auth/calendly"
      via: "CTA button link"
      pattern: "api/auth/calendly"
---

<objective>
Create the /compare landing page showing a feature comparison table of PriCal vs manual Calendly management, plus a time savings narrative section and signup CTA.

Purpose: Help potential users understand why PriCal is better than manually managing their Calendly allowlist.
Output: A fully styled static comparison page at /compare.
</objective>

<execution_context>
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/privacy/page.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /compare page with feature table, time savings narrative, and CTA</name>
  <files>src/app/compare/page.tsx</files>
  <read_first>
    - src/app/privacy/page.tsx (style reference for public content pages - nav, footer, container layout)
    - src/app/page.tsx (landing page CTA style reference - Button component usage)
  </read_first>
  <action>
Create `src/app/compare/page.tsx` as a Next.js server component following the same layout pattern as `src/app/privacy/page.tsx` (nav bar with Shield logo + Sign In/Get Started buttons, container main content, footer with Privacy/Terms/Contact links).

Per D-01: Include BOTH a feature comparison table AND a time savings narrative section below it.
Per D-02: NO pricing information anywhere on this page.
Per D-03: CTA button at the bottom linking to `/api/auth/calendly` with text "Get Started Free" matching landing page style.

**Metadata:**
```typescript
export const metadata: Metadata = {
  title: 'PriCal vs Manual Calendly Management | PriCal',
  description: 'Compare PriCal automated booking protection against managing your Calendly manually',
}
```

**Page structure:**

1. **Hero section** - heading: "PriCal vs. Manual Calendly Management", subtext explaining this is a comparison of automated protection vs doing it yourself.

2. **Feature comparison table** - use a styled HTML table (not the shadcn Table component to keep it simple). Columns: "Feature", "PriCal", "Manual". Use CheckCircle (green) and X (red) icons from lucide-react for yes/no cells. Rows to include:
   - Automatic booking screening (PriCal: yes, Manual: no)
   - Email allowlist management (PriCal: yes, Manual: spreadsheet/memory)
   - Instant cancellation of unauthorized bookings (PriCal: yes, Manual: manual review)
   - Activity log of all booking decisions (PriCal: yes, Manual: no)
   - Email notifications for approved/rejected bookings (PriCal: yes, Manual: no)
   - CSV import/export of allowlist (PriCal: yes, Manual: N/A)
   - Works 24/7 without intervention (PriCal: yes, Manual: no)
   - Guest email checking (PriCal: configurable modes, Manual: not possible)

3. **Time savings narrative section** - a section below the table with heading "Save Hours Every Week". Include 2-3 short paragraphs explaining:
   - Without PriCal: manually checking each booking, cross-referencing against your list, cancelling unwanted ones, risk of missing one
   - With PriCal: set up your allowlist once, PriCal handles everything automatically in real-time, get notified only when you need to know
   - Frame around "focus on what matters" - meetings with the right people, not calendar management

4. **CTA section** - centered section with heading "Ready to protect your calendar?", subtext, and a large "Get Started Free" Button linking to `/api/auth/calendly`. Match the landing page CTA style using `<a href="/api/auth/calendly"><Button size="lg">Get Started Free</Button></a>`.

**Styling:**
- Use the same nav/footer pattern from privacy/page.tsx exactly (copy the nav and footer JSX)
- Main content: `container py-16` wrapper
- Table: styled with Tailwind borders, alternating row backgrounds using `even:bg-muted/50`
- Time savings section: use `max-w-3xl mx-auto` for readability
- CTA section: `text-center py-16`
- Import Shield, CheckCircle, X from lucide-react
  </action>
  <verify>
    <automated>cd /Users/stacysamuels/Desktop/Protectly && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/app/compare/page.tsx
    - Contains "ComparisonPage" or "ComparePage" as the default export function name
    - Contains feature comparison table with CheckCircle and X icons
    - Contains "Save Hours Every Week" or similar time savings heading
    - Contains CTA linking to "/api/auth/calendly"
    - Does NOT contain any pricing ($, price, /month, tier) per D-02
    - Contains metadata with title including "PriCal vs"
    - `next build` succeeds without errors
  </acceptance_criteria>
  <done>The /compare page renders a feature comparison table with 8+ feature rows, a time savings narrative section, and a Get Started CTA. No pricing appears on the page.</done>
</task>

</tasks>

<verification>
- `next build` completes without errors
- `grep -r "ComparisonPage\|ComparePage" src/app/compare/` returns a match
- `grep -c "CheckCircle" src/app/compare/page.tsx` returns at least 1
- `grep "price\|pricing\|\\\$.*month\|/month\|tier" src/app/compare/page.tsx` returns NO matches (per D-02)
</verification>

<success_criteria>
A visitor can navigate to /compare and see a feature comparison table contrasting PriCal features against manual Calendly management, read a time savings narrative, and click a CTA to sign up. No pricing information appears on the page.
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-pages-documentation/14-01-SUMMARY.md`
</output>
