---
phase: 12-onboarding-empty-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/allowlist-table.tsx
  - src/app/(dashboard)/dashboard/activity/page.tsx
autonomous: true
requirements: [ONBOARD-02]
must_haves:
  truths:
    - "The allowlist page shows a helpful empty state with icon, explanation, and Add email CTA button when no entries exist"
    - "The activity log page shows an empty state explaining that booking events appear once the Calendly webhook is active"
    - "Empty states use a consistent pattern: Lucide icon + heading + descriptive text + primary action CTA"
    - "Empty state tone is helpful and encouraging (per D-05)"
  artifacts:
    - path: "src/components/dashboard/allowlist-table.tsx"
      provides: "Enhanced empty state with CTA button"
      contains: "Add your first"
    - path: "src/app/(dashboard)/dashboard/activity/page.tsx"
      provides: "Enhanced empty state with webhook explanation"
      contains: "webhook"
  key_links:
    - from: "src/components/dashboard/allowlist-table.tsx"
      to: "empty state rendering"
      via: "entries.length === 0 check"
      pattern: "entries.length === 0"
    - from: "src/app/(dashboard)/dashboard/activity/page.tsx"
      to: "empty state rendering"
      via: "attempts.length === 0 check"
      pattern: "attempts.length === 0"
---

<objective>
Enhance empty states on the allowlist and activity pages to be helpful, encouraging, and actionable instead of bare minimal placeholders.

Purpose: New users landing on empty pages need guidance, not blank content. Per D-04, every empty state should have an icon, 1-2 line explanation, and a primary action CTA. Per D-05, tone should be encouraging.
Output: Updated empty state UI in allowlist table and activity page.
</objective>

<execution_context>
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing empty state patterns to enhance -->

From src/components/dashboard/allowlist-table.tsx (current empty state, lines 84-93):
```typescript
if (entries.length === 0) {
  return (
    <div className="text-center py-12">
      <Users className="h-12 w-12 mx-auto mb-4 text-muted-foreground opacity-50" />
      <h3 className="text-lg font-semibold mb-2">No approved emails yet</h3>
      <p className="text-muted-foreground">
        Add your first email to start protecting your calendar.
      </p>
    </div>
  )
}
```

From src/app/(dashboard)/dashboard/activity/page.tsx (current empty state, lines 122-129):
```typescript
{attempts.length === 0 ? (
  <div className="text-center py-12">
    <Activity className="h-12 w-12 mx-auto mb-4 text-muted-foreground opacity-50" />
    <h3 className="text-lg font-semibold mb-2">No activity yet</h3>
    <p className="text-muted-foreground">
      When someone tries to book a meeting, you'll see it here.
    </p>
  </div>
) : ( ... )}
```

From src/components/dashboard/add-email-dialog.tsx:
```typescript
export function AddEmailDialog({ allowlistId }: { allowlistId: string }): JSX.Element
// Renders a Button with Plus icon as trigger
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance allowlist and activity empty states with CTAs</name>
  <files>src/components/dashboard/allowlist-table.tsx, src/app/(dashboard)/dashboard/activity/page.tsx</files>
  <read_first>
    - src/components/dashboard/allowlist-table.tsx
    - src/app/(dashboard)/dashboard/activity/page.tsx
    - src/components/dashboard/add-email-dialog.tsx
    - src/app/(dashboard)/dashboard/allowlist/page.tsx
  </read_first>
  <action>
    1. In `src/components/dashboard/allowlist-table.tsx`, update the empty state block (the `if (entries.length === 0)` return, currently lines 84-93) to include the AddEmailDialog CTA button and more encouraging copy per D-04 and D-05:

       Replace the existing empty state with:
       ```tsx
       if (entries.length === 0) {
         return (
           <div className="text-center py-12">
             <div className="flex h-16 w-16 items-center justify-center rounded-full bg-primary/10 mx-auto mb-4">
               <Users className="h-8 w-8 text-primary" />
             </div>
             <h3 className="text-lg font-semibold mb-2">No approved emails yet</h3>
             <p className="text-muted-foreground mb-6 max-w-sm mx-auto">
               Add your first approved contact to start protecting your calendar. Only people on this list will be able to book meetings with you.
             </p>
             <AddEmailDialog allowlistId={allowlistId} />
           </div>
         )
       }
       ```

       This requires importing AddEmailDialog. Check if it is already imported in the file -- it is NOT currently imported. Add:
       ```typescript
       import { AddEmailDialog } from '@/components/dashboard/add-email-dialog'
       ```

       The `allowlistId` prop is already available as it is passed to AllowlistTable.

       Changes from current:
       - Icon now has a colored circular background (bg-primary/10) instead of opacity-50
       - Copy is more descriptive and encouraging (per D-05)
       - Added max-w-sm mx-auto for better text readability
       - Added mb-6 spacing before the CTA
       - Added the AddEmailDialog CTA button (the key missing piece per ONBOARD-02)

    2. In `src/app/(dashboard)/dashboard/activity/page.tsx`, update the empty state block (currently lines 122-129) inside the Card to include better explanation about webhooks and a link CTA:

       Replace the existing empty state with:
       ```tsx
       {attempts.length === 0 ? (
         <div className="text-center py-12">
           <div className="flex h-16 w-16 items-center justify-center rounded-full bg-primary/10 mx-auto mb-4">
             <Activity className="h-8 w-8 text-primary" />
           </div>
           <h3 className="text-lg font-semibold mb-2">No activity yet</h3>
           <p className="text-muted-foreground mb-2 max-w-sm mx-auto">
             Booking attempts will appear here once your Calendly webhook is active and someone tries to schedule a meeting.
           </p>
           <p className="text-sm text-muted-foreground max-w-sm mx-auto">
             Each booking is automatically checked against your allowlist.
           </p>
         </div>
       ) : ( ... keep existing list rendering ... )}
       ```

       Changes from current:
       - Icon now has a colored circular background (bg-primary/10) matching the allowlist pattern
       - Copy explicitly mentions "Calendly webhook" per roadmap SC4
       - Added second line explaining what happens (per D-05 encouraging tone)
       - Added max-w-sm mx-auto for text readability

       No new imports needed -- Activity and all other icons are already imported.
  </action>
  <verify>
    <automated>cd /Users/stacysamuels/Desktop/Protectly && npx tsc --noEmit --pretty 2>&1 | head -30 && grep -q "AddEmailDialog" src/components/dashboard/allowlist-table.tsx && grep -q "webhook" src/app/\(dashboard\)/dashboard/activity/page.tsx && grep -q "bg-primary/10" src/components/dashboard/allowlist-table.tsx && grep -q "bg-primary/10" src/app/\(dashboard\)/dashboard/activity/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AddEmailDialog" src/components/dashboard/allowlist-table.tsx` finds both the import and the JSX usage
    - `grep "bg-primary/10" src/components/dashboard/allowlist-table.tsx` confirms styled icon background
    - `grep "bg-primary/10" src/app/\(dashboard\)/dashboard/activity/page.tsx` confirms styled icon background
    - `grep -i "webhook" src/app/\(dashboard\)/dashboard/activity/page.tsx` confirms webhook explanation text
    - `grep "max-w-sm" src/components/dashboard/allowlist-table.tsx` confirms text readability constraint
    - `grep "Add your first approved contact" src/components/dashboard/allowlist-table.tsx` confirms encouraging copy
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>Allowlist empty state shows icon with colored background, encouraging description, and AddEmailDialog CTA button. Activity empty state shows icon with colored background and explains webhook-based activity tracking. Both follow the D-04 pattern consistently.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no TypeScript errors
2. `grep -c "bg-primary/10" src/components/dashboard/allowlist-table.tsx src/app/\(dashboard\)/dashboard/activity/page.tsx` -- consistent icon styling in both files
3. `grep "AddEmailDialog" src/components/dashboard/allowlist-table.tsx` -- CTA button present in allowlist empty state
4. `grep "webhook" src/app/\(dashboard\)/dashboard/activity/page.tsx` -- webhook explanation present in activity empty state
</verification>

<success_criteria>
- Allowlist page empty state has icon (colored bg), explanation text, and "Add Email" CTA button (not a blank table)
- Activity page empty state has icon (colored bg) and explains that booking events appear once webhook is active
- Both empty states use consistent D-04 pattern: icon in circular bg + heading + descriptive text + CTA
- Tone is helpful and encouraging per D-05
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-empty-states/12-02-SUMMARY.md`
</output>
