---
phase: 07-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/logger.ts
  - src/lib/logger.test.ts
  - next.config.js
  - src/app/api/webhooks/calendly/route.ts
  - src/app/api/auth/calendly/callback/route.ts
  - src/app/api/webhooks/stripe/route.ts
  - src/lib/calendly.ts
  - src/app/api/billing/checkout/route.ts
  - src/app/api/billing/portal/route.ts
autonomous: true
requirements:
  - OBS-03

must_haves:
  truths:
    - "All server-side log output is valid JSON with structured fields — no raw console.log output remains in src/"
    - "Logger uses pino with info level in production and debug in development"
    - "pino-pretty is used in development for human-readable output"
    - "Logger module cannot be imported from client components (server-only guard)"
  artifacts:
    - path: "src/lib/logger.ts"
      provides: "pino singleton with server-only guard"
      contains: "import 'server-only'"
    - path: "src/lib/logger.test.ts"
      provides: "Unit tests for logger configuration"
      contains: "describe"
    - path: "next.config.js"
      provides: "serverExternalPackages for pino"
      contains: "serverExternalPackages"
  key_links:
    - from: "src/app/api/webhooks/calendly/route.ts"
      to: "src/lib/logger.ts"
      via: "import { logger } from '@/lib/logger'"
      pattern: "logger\\.(info|error|warn)"
    - from: "src/app/api/auth/calendly/callback/route.ts"
      to: "src/lib/logger.ts"
      via: "import { logger } from '@/lib/logger'"
      pattern: "logger\\.(info|error)"
---

<objective>
Install pino structured logger, create the logger singleton, configure next.config.js, and replace all 43 console.log/error calls across 6 source files with structured logger calls.

Purpose: Replaces unstructured console output with JSON-structured logging that is queryable in Vercel logs, making production debugging possible. This is the foundation for all subsequent observability work.
Output: src/lib/logger.ts singleton, logger.test.ts, updated next.config.js, all 6 source files migrated from console to logger.
</objective>

<execution_context>
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability/07-CONTEXT.md
@.planning/phases/07-observability/07-RESEARCH.md

<interfaces>
<!-- Current next.config.js structure (CommonJS, must remain CommonJS): -->
From next.config.js:
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'd3v0px0pttie1i.cloudfront.net',
        pathname: '/uploads/**',
      },
    ],
  },
}
module.exports = nextConfig
```

<!-- Existing provider pattern to follow: -->
From src/components/providers/query-provider.tsx:
```typescript
'use client'
// ...
export function QueryProvider({ children }: { children: React.ReactNode }) { ... }
```

<!-- Layout wrapping pattern: -->
From src/app/layout.tsx:
```typescript
<QueryProvider>
  {children}
  <Toaster />
</QueryProvider>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pino, create logger singleton, configure next.config.js, and write unit tests</name>
  <files>src/lib/logger.ts, src/lib/logger.test.ts, next.config.js, package.json</files>
  <read_first>
    - next.config.js (current state to preserve existing config)
    - src/lib/session.ts (reference for lib singleton pattern)
    - vitest.config.ts (test configuration)
  </read_first>
  <behavior>
    - Test 1: logger is defined and has .info, .error, .warn, .debug methods
    - Test 2: logger level is 'info' when NODE_ENV=production
    - Test 3: logger level is 'debug' when NODE_ENV=development
    - Test 4: logger.child() returns a child logger with bound fields
  </behavior>
  <action>
    1. Install packages:
       ```bash
       npm install pino
       npm install -D pino-pretty
       ```

    2. Create `src/lib/logger.ts`:
       ```typescript
       import 'server-only'
       import pino from 'pino'

       export const logger = pino({
         level: process.env.NODE_ENV === 'production' ? 'info' : 'debug',
         ...(process.env.NODE_ENV !== 'production' && {
           transport: {
             target: 'pino-pretty',
             options: { colorize: true, translateTime: 'SYS:standard' },
           },
         }),
       })
       ```

    3. Create `src/lib/logger.test.ts` — mock 'server-only' as empty module via vi.mock('server-only', () => ({})), mock pino module to return a mock logger, verify configuration passed to pino() matches expected values for production and development.

    4. Update `next.config.js` — add `serverExternalPackages: ['pino', 'pino-pretty']` to the nextConfig object, immediately after `reactStrictMode: true`. Keep all existing config (images, etc.) unchanged. Keep CommonJS format.

    Per D-03: production log level defaults to 'info'.
  </action>
  <verify>
    <automated>npx vitest run src/lib/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/logger.ts exists and contains `import 'server-only'`
    - src/lib/logger.ts contains `level: process.env.NODE_ENV === 'production' ? 'info' : 'debug'`
    - src/lib/logger.ts contains `target: 'pino-pretty'`
    - next.config.js contains `serverExternalPackages: ['pino', 'pino-pretty']`
    - next.config.js still contains `module.exports = nextConfig`
    - package.json dependencies contains "pino"
    - package.json devDependencies contains "pino-pretty"
    - npx vitest run src/lib/logger.test.ts exits 0
  </acceptance_criteria>
  <done>Logger singleton created with server-only guard, pino-pretty for dev, JSON for production. next.config.js updated with serverExternalPackages. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 43 console.log/error calls with structured logger calls across 6 files</name>
  <files>
    src/app/api/webhooks/calendly/route.ts,
    src/app/api/auth/calendly/callback/route.ts,
    src/app/api/webhooks/stripe/route.ts,
    src/lib/calendly.ts,
    src/app/api/billing/checkout/route.ts,
    src/app/api/billing/portal/route.ts
  </files>
  <read_first>
    - src/lib/logger.ts (the logger just created)
    - src/app/api/webhooks/calendly/route.ts (25 console calls to replace)
    - src/app/api/auth/calendly/callback/route.ts (9 console calls to replace)
    - src/app/api/webhooks/stripe/route.ts (3 console calls to replace)
    - src/lib/calendly.ts (4 console calls to replace)
    - src/app/api/billing/checkout/route.ts (1 console call to replace)
    - src/app/api/billing/portal/route.ts (1 console call to replace)
    - src/app/api/webhooks/calendly/route.test.ts (existing tests — add logger mock)
    - src/app/api/webhooks/stripe/route.test.ts (existing tests — add logger mock)
  </read_first>
  <action>
    Add `import { logger } from '@/lib/logger'` to the top of each file (after existing imports, following the @/ alias convention).

    Replace every console.log/error call following these rules per D-04 (IDs only, no PII):

    **src/app/api/webhooks/calendly/route.ts** (25 calls):
    - `console.log('[Calendly Webhook] Received webhook request')` -> `logger.info('webhook request received')`
    - `console.log('[Calendly Webhook] Signature header present:', !!signatureHeader)` -> `logger.info({ signaturePresent: !!signatureHeader }, 'webhook signature check')`
    - `console.error('[Calendly Webhook] Invalid webhook signature')` -> `logger.error('invalid webhook signature')`
    - `console.error('[Calendly Webhook] Webhook timestamp outside tolerance')` -> `logger.error('webhook timestamp outside tolerance')`
    - `console.log('[Calendly Webhook] Signature verified successfully')` -> `logger.info('webhook signature verified')`
    - `console.log('[Calendly Webhook] Event type:', payload.event)` -> `logger.info({ eventType: payload.event }, 'webhook event type')`
    - `console.log('[Calendly Webhook] Ignoring non-invitee.created event')` -> `logger.info({ eventType: payload.event }, 'ignoring non-invitee.created event')`
    - `console.log('[Calendly Webhook] Duplicate event detected, skipping:', inviteeUri)` -> `logger.info({ inviteeUri }, 'duplicate event detected, skipping')`
    - `console.log('[Calendly Webhook] Processing booking:', {...})` -> `logger.info({ inviteeUri, eventUri, createdBy }, 'processing booking')` (log IDs only, NOT email/name per D-04)
    - `console.log('[Calendly Webhook] Guest emails:', guestEmails)` -> `logger.info({ guestCount: guestEmails.length }, 'guest emails extracted')` (count only, NOT emails per D-04)
    - `console.error('[Calendly Webhook] User not found for webhook:', createdBy)` -> `logger.error({ createdBy }, 'user not found for webhook')`
    - `console.log('[Calendly Webhook] Found user:', user.id)` -> `logger.info({ userId: user.id }, 'user found')`
    - `console.log('[Calendly Webhook] Allowlist check:', {...})` -> `logger.info({ userId: user.id, action: 'allowlist_check' }, 'checking allowlist')` (no email data per D-04)
    - `console.log('[Calendly Webhook] Booking APPROVED...')` -> `logger.info({ userId: user.id, action: 'booking_approved', eventUri }, 'booking approved')`
    - `console.log('[Calendly Webhook] Response: approved')` -> `logger.info({ action: 'booking_approved' }, 'response sent')`
    - `console.log('[Calendly Webhook] Booking NOT approved...')` -> `logger.info({ userId: user.id, action: 'booking_rejected', eventUri }, 'booking rejected, attempting cancellation')`
    - `console.log('[Calendly Webhook] Cancelling event URI:', eventUri)` -> `logger.info({ eventUri, action: 'cancel_booking' }, 'cancelling event')`
    - `console.log('[Calendly Webhook] Waiting 4 seconds...')` -> `logger.info({ action: 'cancel_booking' }, 'waiting before cancellation')`
    - `console.log('[Calendly Webhook] Cancellation successful')` -> `logger.info({ action: 'cancel_booking' }, 'cancellation successful')`
    - `console.log('[Calendly Webhook] Response: rejected (cancelled successfully)')` -> `logger.info({ action: 'booking_rejected' }, 'response sent, cancellation successful')`
    - `console.error('[Calendly Webhook] Failed to cancel booking:', cancelError)` -> `logger.error({ err: cancelError, action: 'cancel_booking' }, 'failed to cancel booking')`
    - `console.error('[Calendly Webhook] Cancel error details:', {...})` -> remove (redundant with previous error log, details in err object)
    - `console.log('[Calendly Webhook] Response: rejected (cancellation failed)')` -> `logger.warn({ action: 'booking_rejected' }, 'response sent, cancellation failed')`
    - `console.error('[Calendly Webhook] Webhook processing error:', error)` -> `logger.error({ err: error, action: 'process_webhook' }, 'webhook processing failed')`
    - `console.error('[Calendly Webhook] Error details:', {...})` -> remove (redundant with previous error log)

    **src/app/api/auth/calendly/callback/route.ts** (9 calls):
    - `console.error("Calendly OAuth error:", error)` -> `logger.error({ err: error, action: 'oauth_exchange' }, 'calendly OAuth token exchange failed')`
    - `console.log("[Calendly OAuth] Webhook URL configured as:", webhookUrl)` -> `logger.info({ action: 'oauth_callback' }, 'webhook URL configured')`  (no URL value per D-04)
    - `console.log("[Calendly OAuth] New user created:", user.id)` -> `logger.info({ userId: user.id, action: 'signup' }, 'new user created')`
    - `console.log("[Calendly OAuth] Existing user updated:", user.id)` -> `logger.info({ userId: user.id, action: 'login' }, 'existing user updated')`
    - `console.log("[Calendly OAuth] Creating webhook subscription...")` -> `logger.info({ userId: user.id, action: 'webhook_setup' }, 'creating webhook subscription')`
    - `console.log("[Calendly OAuth] Webhook subscription created:...", )` -> `logger.info({ userId: user.id, action: 'webhook_setup' }, 'webhook subscription created')`
    - `console.log("[Calendly OAuth] Webhook subscription already exists")` -> `logger.info({ userId: user.id, action: 'webhook_setup' }, 'webhook subscription already exists')`
    - `console.error("[Calendly OAuth] Failed to create webhook subscription:", ...)` -> `logger.error({ err: webhookError, userId: user.id, action: 'webhook_setup' }, 'failed to create webhook subscription')`
    - `console.error("OAuth callback error:", error)` -> `logger.error({ err: error, action: 'oauth_callback' }, 'OAuth callback failed')`

    **src/app/api/webhooks/stripe/route.ts** (3 calls):
    - `console.error('Webhook signature verification failed:', err)` -> `logger.error({ err, action: 'stripe_webhook' }, 'webhook signature verification failed')`
    - `console.log('[Stripe Webhook] Duplicate event detected, skipping:', event.id)` -> `logger.info({ eventId: event.id, action: 'stripe_webhook' }, 'duplicate event detected, skipping')`
    - `console.error('Webhook processing error:', error)` -> `logger.error({ err: error, action: 'stripe_webhook' }, 'webhook processing error')`

    **src/lib/calendly.ts** (4 calls):
    - `console.log("[Calendly API] Cancelling event:", {...})` -> `logger.info({ eventUri, action: 'cancel_event' }, 'cancelling calendly event')` (no token data)
    - `console.log("[Calendly API] Cancel response status:", response.status)` -> `logger.info({ status: response.status, action: 'cancel_event' }, 'cancel response received')`
    - `console.log("[Calendly API] Cancel response data:", data)` -> `logger.info({ action: 'cancel_event' }, 'cancel response data received')` (no payload per D-04)
    - `console.error("[Calendly API] Cancel request failed:", {...})` -> `logger.error({ err: error, action: 'cancel_event' }, 'cancel request failed')`

    **src/app/api/billing/checkout/route.ts** (1 call):
    - `console.error('Failed to create checkout session:', error)` -> `logger.error({ err: error, action: 'create_checkout' }, 'failed to create checkout session')`

    **src/app/api/billing/portal/route.ts** (1 call):
    - `console.error('Failed to create portal session:', error)` -> `logger.error({ err: error, action: 'create_portal' }, 'failed to create portal session')`

    **Test file updates:**
    - In `src/app/api/webhooks/calendly/route.test.ts`: add `vi.mock('@/lib/logger', () => ({ logger: { info: vi.fn(), error: vi.fn(), warn: vi.fn(), debug: vi.fn(), child: vi.fn(() => ({ info: vi.fn(), error: vi.fn(), warn: vi.fn(), debug: vi.fn() })) } }))` at the top with other mocks
    - In `src/app/api/webhooks/stripe/route.test.ts`: add the same logger mock

    Each file must be migrated atomically — no file should have a mix of console.log and logger calls when done.
  </action>
  <verify>
    <automated>npx vitest run && grep -r "console\.\(log\|error\|warn\)" src/app src/lib --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "console\.\(log\|error\|warn\)" src/app src/lib --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules` returns zero results
    - src/app/api/webhooks/calendly/route.ts contains `import { logger } from '@/lib/logger'`
    - src/app/api/webhooks/calendly/route.ts contains `logger.info(`
    - src/app/api/auth/calendly/callback/route.ts contains `import { logger } from '@/lib/logger'`
    - src/app/api/webhooks/stripe/route.ts contains `import { logger } from '@/lib/logger'`
    - src/lib/calendly.ts contains `import { logger } from '@/lib/logger'`
    - src/app/api/billing/checkout/route.ts contains `import { logger } from '@/lib/logger'`
    - src/app/api/billing/portal/route.ts contains `import { logger } from '@/lib/logger'`
    - src/app/api/webhooks/calendly/route.test.ts contains `vi.mock('@/lib/logger'`
    - src/app/api/webhooks/stripe/route.test.ts contains `vi.mock('@/lib/logger'`
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>All 43 console.log/error calls replaced with structured logger calls. No console.log/error remains in non-test source files. All existing tests pass with logger mock added. Log messages comply with D-04 (IDs only, no PII).</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (including logger.test.ts and existing webhook tests with logger mock)
2. `grep -r "console\.\(log\|error\|warn\)" src/app src/lib --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules` — returns zero results
3. `grep "serverExternalPackages" next.config.js` — returns match containing pino
4. `grep "import 'server-only'" src/lib/logger.ts` — returns match
</verification>

<success_criteria>
- pino installed as dependency, pino-pretty as devDependency
- src/lib/logger.ts exports pino singleton with server-only guard
- next.config.js contains serverExternalPackages: ['pino', 'pino-pretty']
- Zero console.log/error/warn calls remain in non-test source files under src/
- All vitest tests pass
- Log calls use structured objects with action fields per D-03 and D-04
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability/07-01-SUMMARY.md`
</output>
