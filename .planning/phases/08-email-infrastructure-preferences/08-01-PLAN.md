---
phase: 08-email-infrastructure-preferences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/env.ts
  - src/lib/email.ts
  - src/lib/email.test.ts
  - src/emails/layout/base-layout.tsx
  - src/emails/booking-approved.tsx
  - src/emails/booking-rejected.tsx
  - src/emails/trial-expiry-3days.tsx
  - src/emails/trial-expiry-1day.tsx
  - src/emails/trial-expired.tsx
  - src/emails/booking-approved.test.tsx
  - src/emails/booking-rejected.test.tsx
  - src/emails/trial-expiry-3days.test.tsx
  - src/emails/trial-expiry-1day.test.tsx
  - src/emails/trial-expired.test.tsx
autonomous: true
requirements:
  - EMAIL-01

must_haves:
  truths:
    - "sendEmail() calls Resend SDK with correct from/to/subject/react arguments"
    - "sendEmail() throws an Error when Resend returns an error object"
    - "All five email templates render to HTML without throwing"
    - "Email templates contain expected content strings matching their props"
    - "RESEND_API_KEY and EMAIL_FROM are validated in env.ts as optional server vars"
  artifacts:
    - path: "src/lib/email.ts"
      provides: "Resend singleton and sendEmail utility"
      contains: "server-only"
    - path: "src/emails/layout/base-layout.tsx"
      provides: "Shared email wrapper component"
      contains: "BaseLayout"
    - path: "src/emails/booking-approved.tsx"
      provides: "Approved booking email template"
      contains: "BookingApproved"
    - path: "src/emails/booking-rejected.tsx"
      provides: "Rejected booking email template"
      contains: "BookingRejected"
    - path: "src/emails/trial-expiry-3days.tsx"
      provides: "3-day trial warning template"
      contains: "TrialExpiry3Days"
    - path: "src/emails/trial-expiry-1day.tsx"
      provides: "1-day trial warning template"
      contains: "TrialExpiry1Day"
    - path: "src/emails/trial-expired.tsx"
      provides: "Trial expired notification template"
      contains: "TrialExpired"
  key_links:
    - from: "src/lib/email.ts"
      to: "src/env.ts"
      via: "env.RESEND_API_KEY and env.EMAIL_FROM imports"
      pattern: "env\\.RESEND_API_KEY"
    - from: "src/emails/booking-approved.tsx"
      to: "src/emails/layout/base-layout.tsx"
      via: "BaseLayout wrapper import"
      pattern: "import.*BaseLayout"
---

<objective>
Install Resend and React Email packages, create the email sending singleton, and build all five email templates with unit tests.

Purpose: Establishes the email infrastructure that Phases 9 and 10 will use to send booking notifications and trial warnings.
Output: Working sendEmail() utility, five tested React Email templates, env var validation for RESEND_API_KEY and EMAIL_FROM.
</objective>

<execution_context>
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-email-infrastructure-preferences/08-CONTEXT.md
@.planning/phases/08-email-infrastructure-preferences/08-RESEARCH.md

@src/env.ts
@src/lib/logger.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages, add env vars, create sendEmail utility with tests</name>
  <files>src/env.ts, src/lib/email.ts, src/lib/email.test.ts</files>
  <read_first>
    - src/env.ts
    - src/lib/logger.ts (for server-only guard pattern)
    - src/lib/prisma.ts (for singleton pattern reference)
  </read_first>
  <behavior>
    - Test 1: sendEmail calls resend.emails.send with from="PriCal Notifications <notifications@prical.io>", to, subject, react args
    - Test 2: sendEmail throws Error with message containing "Email delivery failed" when Resend returns { error: { message: "test error" } }
    - Test 3: sendEmail does not throw when Resend returns { data: { id: "..." }, error: null }
  </behavior>
  <action>
    1. Install packages: `npm install resend react-email @react-email/components @react-email/render`

    2. Add to src/env.ts server section (after the PostHog block, before NODE_ENV):
       ```
       RESEND_API_KEY: z.string().min(1).optional(),
       EMAIL_FROM: z.string().email().optional(),
       ```
       Add to runtimeEnv section:
       ```
       RESEND_API_KEY: process.env.RESEND_API_KEY,
       EMAIL_FROM: process.env.EMAIL_FROM,
       ```
       Per D-07, the default EMAIL_FROM value is notifications@prical.io.

    3. Create src/lib/email.ts:
       - Import 'server-only' as first line (same pattern as logger.ts)
       - Import { Resend } from 'resend'
       - Import { env } from '@/env'
       - Construct Resend singleton: `const resend = new Resend(env.RESEND_API_KEY)`
       - Export async function sendEmail(opts: { to: string; subject: string; react: React.ReactElement })
       - Inside sendEmail: call resend.emails.send with from: `PriCal Notifications <${env.EMAIL_FROM}>` (per D-06, D-07)
       - Spread ...opts after from
       - If error is returned, throw new Error(`Email delivery failed: ${error.message}`)
       - Do NOT wrap in try/catch — let callers handle failures (per research pitfall 2)

    4. Create src/lib/email.test.ts:
       - vi.mock('server-only', () => ({}))
       - vi.mock('@/env', () => ({ env: { RESEND_API_KEY: 'test_key', EMAIL_FROM: 'notifications@prical.io' } }))
       - Mock resend module: vi.mock('resend', () => ({ Resend: vi.fn().mockImplementation(() => ({ emails: { send: vi.fn() } })) }))
       - Write the 3 behavior tests above
       - Verify from field equals 'PriCal Notifications <notifications@prical.io>' per D-06
  </action>
  <verify>
    <automated>npx vitest run src/lib/email.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RESEND_API_KEY" src/env.ts returns 0
    - grep -q "EMAIL_FROM" src/env.ts returns 0
    - grep -q "server-only" src/lib/email.ts returns 0
    - grep -q "sendEmail" src/lib/email.ts returns 0
    - grep -q "PriCal Notifications" src/lib/email.ts returns 0
    - npx vitest run src/lib/email.test.ts passes with 3 tests
  </acceptance_criteria>
  <done>sendEmail utility exports from src/lib/email.ts, env vars validated, 3 unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BaseLayout and all five email templates with render tests</name>
  <files>
    src/emails/layout/base-layout.tsx,
    src/emails/booking-approved.tsx,
    src/emails/booking-rejected.tsx,
    src/emails/trial-expiry-3days.tsx,
    src/emails/trial-expiry-1day.tsx,
    src/emails/trial-expired.tsx,
    src/emails/booking-approved.test.tsx,
    src/emails/booking-rejected.test.tsx,
    src/emails/trial-expiry-3days.test.tsx,
    src/emails/trial-expiry-1day.test.tsx,
    src/emails/trial-expired.test.tsx
  </files>
  <read_first>
    - src/lib/email.ts (just created in Task 1 — for sendEmail signature)
    - .planning/phases/08-email-infrastructure-preferences/08-RESEARCH.md (template structure, BaseLayout pattern, render test pattern)
    - .planning/phases/08-email-infrastructure-preferences/08-CONTEXT.md (D-01 clean minimal style, D-02 warm professional tone, D-03 rejection reason)
  </read_first>
  <behavior>
    - Test per template: render(<Template {...testProps} />) produces HTML string containing expected prop values
    - BookingApproved: HTML contains inviteeName, inviteeEmail, eventTypeName
    - BookingRejected: HTML contains inviteeName, inviteeEmail, rejectionReason (per D-03)
    - TrialExpiry3Days: HTML contains daysRemaining ("3"), userName
    - TrialExpiry1Day: HTML contains "expires tomorrow" or "1 day", userName
    - TrialExpired: HTML contains "expired", userName
  </behavior>
  <action>
    1. Create src/emails/layout/base-layout.tsx:
       - Import Html, Head, Body, Container, Preview, Text from '@react-email/components'
       - Props: { preview?: string; children: React.ReactNode }
       - Clean minimal style per D-01: white background (#ffffff), single-column, max-width 600px, system font stack
       - Include a small "PriCal" text header and a muted footer with "You received this because you have a PriCal account"
       - Export as named export: `export function BaseLayout`

    2. Create src/emails/booking-approved.tsx:
       - Props: { inviteeName: string; inviteeEmail: string; eventTypeName: string; eventTime: string }
       - Subject line (for Phase 9 to use): "Booking confirmed: {inviteeName}"
       - Preview text: "A booking from {inviteeName} was approved"
       - Body per D-02 warm professional tone: "A booking from {inviteeName} ({inviteeEmail}) for {eventTypeName} on {eventTime} has been confirmed. They are on your allowlist."
       - Default export the component

    3. Create src/emails/booking-rejected.tsx:
       - Props: { inviteeName: string; inviteeEmail: string; eventTypeName: string; eventTime: string; rejectionReason: string; addToAllowlistUrl: string }
       - Per D-03: Show specific rejection reason so user can decide whether to add the person
       - Preview text: "A booking from {inviteeName} was cancelled"
       - Body per D-02: "A booking from {inviteeName} ({inviteeEmail}) for {eventTypeName} was cancelled — {rejectionReason}."
       - Include a Button CTA: "Add to allowlist" linking to addToAllowlistUrl
       - Per D-02 tone: "You can add them with one click if this was someone you expected."
       - Default export

    4. Create src/emails/trial-expiry-3days.tsx:
       - Props: { userName: string; trialEndDate: string; upgradeUrl: string }
       - Preview: "Your PriCal trial ends in 3 days"
       - Body: "Hi {userName}, your free trial ends on {trialEndDate}. Upgrade to keep protecting your calendar."
       - Button CTA: "Upgrade now" linking to upgradeUrl
       - Default export

    5. Create src/emails/trial-expiry-1day.tsx:
       - Props: { userName: string; trialEndDate: string; upgradeUrl: string }
       - Preview: "Your PriCal trial expires tomorrow"
       - Body: "Hi {userName}, your trial expires tomorrow ({trialEndDate}). After that, booking protection will be paused."
       - Button CTA: "Upgrade now" linking to upgradeUrl
       - Default export

    6. Create src/emails/trial-expired.tsx:
       - Props: { userName: string; upgradeUrl: string }
       - Preview: "Your PriCal trial has expired"
       - Body: "Hi {userName}, your free trial has expired and your account has been moved to the free plan. Upgrade anytime to resume full protection."
       - Button CTA: "Upgrade now" linking to upgradeUrl
       - Default export

    7. Create render test for each template:
       - Import { render } from '@react-email/render'
       - Import the template component
       - Call render(<Template {...props} />) (it returns a string or Promise<string>)
       - Assert the result contains key prop values
       - One test file per template: src/emails/{template-name}.test.tsx
  </action>
  <verify>
    <automated>npx vitest run src/emails/</automated>
  </verify>
  <acceptance_criteria>
    - ls src/emails/*.tsx | wc -l outputs 6 (5 templates + 1 in layout/)
    - grep -q "BaseLayout" src/emails/layout/base-layout.tsx returns 0
    - grep -q "BookingApproved" src/emails/booking-approved.tsx returns 0
    - grep -q "BookingRejected" src/emails/booking-rejected.tsx returns 0
    - grep -q "rejectionReason" src/emails/booking-rejected.tsx returns 0 (per D-03)
    - grep -q "TrialExpiry3Days" src/emails/trial-expiry-3days.tsx returns 0
    - grep -q "TrialExpiry1Day" src/emails/trial-expiry-1day.tsx returns 0
    - grep -q "TrialExpired" src/emails/trial-expired.tsx returns 0
    - npx vitest run src/emails/ passes with 5 test files, all green
  </acceptance_criteria>
  <done>All five email templates render without error, contain expected content, and match the D-01 clean minimal style with D-02 warm professional tone. BookingRejected includes rejectionReason per D-03.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/email.test.ts src/emails/` — all tests pass
- `grep -q "RESEND_API_KEY" src/env.ts` — env var registered
- `grep -q "EMAIL_FROM" src/env.ts` — env var registered
- `ls src/emails/*.tsx | wc -l` — 5 template files exist
- `ls src/emails/layout/base-layout.tsx` — shared layout exists
</verification>

<success_criteria>
- sendEmail() utility works with mocked Resend SDK (3 tests pass)
- All 5 templates render to HTML with correct prop interpolation (5 test files pass)
- RESEND_API_KEY and EMAIL_FROM are optional env vars in src/env.ts
- No raw process.env usage — all through env.ts
</success_criteria>

<output>
After completion, create `.planning/phases/08-email-infrastructure-preferences/08-01-SUMMARY.md`
</output>
