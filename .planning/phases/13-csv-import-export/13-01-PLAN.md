---
phase: 13-csv-import-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/allowlists/[id]/export/route.ts
  - src/app/api/allowlists/[id]/export/export.test.ts
  - src/components/dashboard/csv-export-button.tsx
  - src/components/dashboard/csv-export-button.test.tsx
  - src/app/(dashboard)/dashboard/allowlist/page.tsx
autonomous: true
requirements: [LIST-02]

must_haves:
  truths:
    - "Any user can click Export CSV and receive a downloadable CSV file"
    - "Export CSV contains email, name, notes, dateAdded columns for all allowlist entries"
    - "CSV fields with commas or quotes are properly escaped"
    - "Export filename follows prical-allowlist-YYYY-MM-DD.csv format per D-05"
    - "Export includes all entries regardless of search/filter state per D-04"
  artifacts:
    - path: "src/app/api/allowlists/[id]/export/route.ts"
      provides: "GET endpoint returning text/csv with Content-Disposition header"
      exports: ["GET"]
    - path: "src/app/api/allowlists/[id]/export/export.test.ts"
      provides: "Vitest tests for export route: auth, ownership, CSV format, escaping"
    - path: "src/components/dashboard/csv-export-button.tsx"
      provides: "Client component: Export CSV button with download trigger"
    - path: "src/components/dashboard/csv-export-button.test.tsx"
      provides: "Vitest tests for CsvExportButton: fetch call, blob download, error handling, loading state"
    - path: "src/app/(dashboard)/dashboard/allowlist/page.tsx"
      provides: "Allowlist page updated with export button"
  key_links:
    - from: "src/components/dashboard/csv-export-button.tsx"
      to: "/api/allowlists/[id]/export"
      via: "fetch GET + blob download"
      pattern: "fetch.*allowlists.*export"
    - from: "src/app/(dashboard)/dashboard/allowlist/page.tsx"
      to: "src/components/dashboard/csv-export-button.tsx"
      via: "component import and render"
      pattern: "CsvExportButton"
---

<objective>
Add CSV export functionality to the allowlist page so any user can download their allowlist entries as a CSV file.

Purpose: Fulfills LIST-02 — users can back up or edit their allowlist data offline. No tier restriction per D-07.
Output: GET export API route, CsvExportButton component, updated allowlist page with export button.
</objective>

<execution_context>
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stacysamuels/Desktop/Protectly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/api/allowlists/[id]/entries/route.ts:
```typescript
// Auth + ownership pattern used consistently across allowlist routes
const user = await getCurrentUser()
if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
const { id } = await params
const allowlist = await prisma.allowlist.findFirst({ where: { id, userId: user.id } })
if (!allowlist) return NextResponse.json({ error: 'Not found' }, { status: 404 })
```

From src/components/dashboard/allowlist-table.tsx:
```typescript
interface AllowlistEntry {
  id: string
  email: string
  name: string | null
  notes: string | null
  expiresAt: Date | null
  createdAt: Date
}

interface AllowlistTableProps {
  entries: AllowlistEntry[]
  allowlistId: string
}
```

From src/app/(dashboard)/dashboard/allowlist/page.tsx:
```typescript
// Server component — passes allowlist.id and allowlist.entries to child components
// Header area has flex layout with title left, AddEmailDialog button right
<div className="flex items-center justify-between">
  <div>
    <h1 className="text-3xl font-bold">Allowlist</h1>
    ...
  </div>
  <AddEmailDialog allowlistId={allowlist.id} />
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CSV export API route with tests</name>
  <files>src/app/api/allowlists/[id]/export/route.ts, src/app/api/allowlists/[id]/export/export.test.ts</files>
  <read_first>
    - src/app/api/allowlists/[id]/entries/route.ts (auth + ownership pattern, prisma imports)
    - src/lib/session.ts (getCurrentUser signature)
    - src/lib/prisma.ts (prisma client import)
  </read_first>
  <behavior>
    - Test 1: GET without auth returns 401 with { error: 'Unauthorized' }
    - Test 2: GET with auth but wrong user's allowlist returns 404
    - Test 3: GET with valid auth returns 200 with Content-Type text/csv
    - Test 4: GET returns Content-Disposition header with filename prical-allowlist-YYYY-MM-DD.csv
    - Test 5: CSV contains header row: email,name,notes,dateAdded
    - Test 6: CSV body rows contain entry data with proper field quoting
    - Test 7: Fields containing commas are escaped (wrapped in double-quotes, inner quotes doubled)
    - Test 8: Empty allowlist returns only the header row
  </behavior>
  <action>
    **Write tests first** in `src/app/api/allowlists/[id]/export/export.test.ts`:
    - Mock `@/lib/session` getCurrentUser and `@/lib/prisma` prisma client
    - Test all 8 behaviors above

    **Then implement** `src/app/api/allowlists/[id]/export/route.ts`:
    - Export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> })
    - Auth check: getCurrentUser() — return 401 if null
    - Ownership check: prisma.allowlist.findFirst({ where: { id, userId: user.id } }) — return 404 if null
    - Fetch all entries: prisma.allowlistEntry.findMany({ where: { allowlistId: id }, orderBy: { createdAt: 'desc' } })
    - Build CSV string:
      - Header: `email,name,notes,dateAdded\n`
      - Each row: wrap ALL four fields in double-quotes, escape inner quotes as `""`
      - Fields: e.email, e.name ?? '', e.notes ?? '', e.createdAt.toISOString().slice(0, 10)
    - Return new NextResponse(csvString) with headers:
      - `Content-Type: text/csv`
      - `Content-Disposition: attachment; filename="prical-allowlist-YYYY-MM-DD.csv"` (per D-05, use server date)
    - Per D-04: export ALL entries, no pagination, no search filter
  </action>
  <verify>
    <automated>npx vitest run src/app/api/allowlists/[id]/export/export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'Content-Type.*text/csv' src/app/api/allowlists/[id]/export/route.ts
    - grep -q 'prical-allowlist-' src/app/api/allowlists/[id]/export/route.ts
    - grep -q 'email,name,notes,dateAdded' src/app/api/allowlists/[id]/export/route.ts
    - grep -q 'replace.*""' src/app/api/allowlists/[id]/export/route.ts (CSV escaping)
    - grep -q 'getCurrentUser' src/app/api/allowlists/[id]/export/route.ts
    - npx vitest run src/app/api/allowlists/[id]/export/export.test.ts exits 0
  </acceptance_criteria>
  <done>GET /api/allowlists/[id]/export returns text/csv with all entries, proper escaping, correct filename format, and auth/ownership guards. All 8 test cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CsvExportButton with tests and add to allowlist page</name>
  <files>src/components/dashboard/csv-export-button.tsx, src/components/dashboard/csv-export-button.test.tsx, src/app/(dashboard)/dashboard/allowlist/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/allowlist/page.tsx (current page layout, where to add button)
    - src/components/dashboard/allowlist-table.tsx (existing component patterns, AllowlistEntry interface)
    - src/components/dashboard/add-email-dialog.tsx (existing button pattern for reference)
    - src/components/ui/button.tsx (Button component API)
    - src/components/ui/button.test.tsx (existing component test pattern: vitest, @testing-library/react, userEvent)
  </read_first>
  <behavior>
    - Test 1: Renders "Export CSV" button with Download icon
    - Test 2: Clicking button calls fetch with correct URL /api/allowlists/{id}/export
    - Test 3: On successful response, creates blob URL and triggers download with prical-allowlist-YYYY-MM-DD.csv filename
    - Test 4: On failed response (!response.ok), shows error toast and does NOT trigger download
    - Test 5: Button shows "Exporting..." text and is disabled during fetch
    - Test 6: Button re-enables after fetch completes (success or failure)
  </behavior>
  <action>
    **Write tests first** in `src/components/dashboard/csv-export-button.test.tsx`:
    - Import { describe, it, expect, vi, beforeEach } from 'vitest'
    - Import { render, screen, waitFor } from '@testing-library/react'
    - Import userEvent from '@testing-library/user-event'
    - Mock global fetch with vi.fn()
    - Mock URL.createObjectURL and URL.revokeObjectURL
    - Mock useToast from @/components/ui/use-toast
    - Test all 6 behaviors above
    - For download trigger test: spy on document.createElement to capture the anchor element click

    **Then create** `src/components/dashboard/csv-export-button.tsx`:
    - 'use client' component
    - Props: `{ allowlistId: string }`
    - State: `exporting` boolean (for loading state)
    - onClick handler:
      1. Set exporting=true
      2. fetch(`/api/allowlists/${allowlistId}/export`)
      3. If !response.ok, show error toast and return
      4. Get blob from response
      5. Create object URL, create anchor element, set href + download attribute
      6. download filename: `prical-allowlist-${new Date().toISOString().slice(0, 10)}.csv` (per D-05)
      7. Click anchor, revoke object URL
      8. Set exporting=false in finally block
    - Render: `<Button variant="outline" disabled={exporting} onClick={handleExport}><Download className="mr-2 h-4 w-4" />{exporting ? 'Exporting...' : 'Export CSV'}</Button>`
    - Import Download icon from lucide-react, Button from @/components/ui/button, useToast from @/components/ui/use-toast

    **Update** `src/app/(dashboard)/dashboard/allowlist/page.tsx`:
    - Import CsvExportButton from @/components/dashboard/csv-export-button
    - In the header area, change the single AddEmailDialog button to a flex row with gap-2:
      ```
      <div className="flex items-center gap-2">
        <CsvExportButton allowlistId={allowlist.id} />
        <AddEmailDialog allowlistId={allowlist.id} />
      </div>
      ```
    - Per D-07: export available to ALL users, no tier check needed
    - Pass user.subscriptionTier is NOT needed for export (no gating)
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/csv-export-button.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'use client'" src/components/dashboard/csv-export-button.tsx
    - grep -q 'prical-allowlist-' src/components/dashboard/csv-export-button.tsx
    - grep -q 'Export CSV' src/components/dashboard/csv-export-button.tsx
    - grep -q 'Download' src/components/dashboard/csv-export-button.tsx
    - grep -q 'CsvExportButton' src/app/(dashboard)/dashboard/allowlist/page.tsx
    - grep -q 'allowlistId' src/components/dashboard/csv-export-button.tsx
    - npx vitest run src/components/dashboard/csv-export-button.test.tsx exits 0
  </acceptance_criteria>
  <done>CsvExportButton component renders an "Export CSV" button with Download icon. Clicking it fetches the export route, triggers browser download with correct filename. Button appears on the allowlist page header next to Add Email. Available to all users per D-07. All 6 component test cases pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/api/allowlists/[id]/export/export.test.ts` passes all test cases
- `npx vitest run src/components/dashboard/csv-export-button.test.tsx` passes all test cases
- `npx next build` completes without errors
- Export route returns proper CSV with Content-Type and Content-Disposition headers
- All allowlist entries included in export regardless of search state (D-04)
- Filename format is prical-allowlist-YYYY-MM-DD.csv (D-05)
</verification>

<success_criteria>
- GET /api/allowlists/[id]/export returns text/csv with email,name,notes,dateAdded columns
- CsvExportButton visible on allowlist page for all users (D-07)
- CSV fields properly escaped (commas, quotes handled)
- All tests pass (route tests + component tests), build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-csv-import-export/13-01-SUMMARY.md`
</output>
